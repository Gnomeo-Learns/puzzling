import { defineConfig } from 'astro/config';

// Update 'puzzle-strats' to match your GitHub repo name
export default defineConfig({
  site: 'https://gnomeo-learns.github.io',
  base: '/puzzling',
});
